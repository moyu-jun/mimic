[package]
name = "mimic-elevated-helper"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
mimic-elevation-protocol = { path = "../elevation-protocol" }
sha2 = "0.10.9"

[target."cfg(windows)".dependencies]
windows-sys = { version = "0.59", features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_System_SystemInformation",
    "Win32_System_Threading",
] }
